# Copy to .env and adjust. If .env doesn't exist, docker-compose falls back
# to the dev defaults below (see docker-compose.yml).

POSTGRES_PASSWORD=postgres
JWT_SIGNING_KEY=CHANGE_ME_dev_only_signing_key_min_32_chars
